# Pin Node 20 for Railway/Nixpacks (cross-env requires Node >=20)
[phases.setup]
nixPkgs = ["nodejs_20"]

# Use npm install instead of npm ci to avoid lock file sync issues
# (Node 20 resolves yaml@2.8.2 but local lock file may not include it)
[phases.install]
cmds = ["npm install"]
